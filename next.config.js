/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ['scontent.fsgn2-8.fna.fbcdn.net'],
  },
}

module.exports = nextConfig
